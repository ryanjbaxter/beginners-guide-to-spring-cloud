spring:
  cloud:
    gateway:
      routes:
      - id: nameRoot
        uri: lb://name
        predicates:
        - Path=/name/**
        filters:
        - StripPrefix
      - id: greeting
        uri: lb://greeting
        predicates:
        - Path=/greeting/**
        filters:
        - StripPrefix
